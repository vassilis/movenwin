<div class="container">
	<div class="jumbotron">
		<h2>Στείλε πρόσκληση σε χρήστη <small> ( +150 )</small></h2>
		<%= form_tag("/users/send_invitation",  method:"put") do %>
			<div class="form-group">
				<input type="text" name="ticket" class="form-control" placeholder='e-mail' style="width:600px"/>
			</div>
			<%=submit_tag('Αποστολή', :class=>"btn btn-primary") %>
		<%end%>
	</div>
</div>